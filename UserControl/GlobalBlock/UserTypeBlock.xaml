<UserControl x:Class="sbid.UserControl.UserTypeBlock"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:sbid.UserControl"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <ContentControl Width="130"
                    MinWidth="50"
                    Height="130"
                    MinHeight="50"
                    Name="contentControl"
                    Template="{StaticResource DesignerItemTemplate}">
        <!--ContentControl的Content放具体的图形-->
        <!--把IsHitTestVisible设置为False,看起来没有变化,但操作上已经把他完全忽视了,不触发事件,可以直接点到它下面的东西-->
        <Path Fill="Blue" Stretch="Fill" IsHitTestVisible="False">
            <Path.Data>
                <!--这里的Rect没啥用，外面一层ContentControl设置的限制了它-->
                <RectangleGeometry Rect="100,100,100,100"/>
            </Path.Data>
        </Path>
        <ContentControl.ContextMenu>
            <ContextMenu>
                <MenuItem Name="menuEdit" Header="编辑" Click="Menu_Click_Edit"/>
                <MenuItem Name="menuDelete" Header="删除" Click="Menu_Click_Delete"/>
            </ContextMenu>
        </ContentControl.ContextMenu>
    </ContentControl>
</UserControl>
